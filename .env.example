# Порт Fastify (в проде задаётся платформой)
PORT=3000

# Строка подключения PostgreSQL (Neon). Если не задана — используется PGlite (тесты/локальный dev).
# Формат: postgresql://user:password@host/db?sslmode=require
DATABASE_URL=
