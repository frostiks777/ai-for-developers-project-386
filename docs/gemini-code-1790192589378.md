# TODO: Calendar Slots App — Frontend Pages & UI Forms Backlog

> **Контекст репозитория:** Алгоритмический движок слотов, буферы, валидация пересечений и изоляция в БД уже реализованы на бэкенде. Данный документ содержит спецификацию недостающих UI-страниц, интерактивных форм и компонентов для автономной реализации ИИ-агентом.

---

## 1. Публичный флоу бронирования (Public Booking Flow) — Приоритет P0

### 1.1. Страница выбора слота: `/book/:slug` (или `/:username/:eventType`)
- [ ] **Компонент `TimezoneSelector`**
  - [ ] Автоматическое определение часового пояса клиента через `Intl.DateTimeFormat().resolvedOptions().timeZone`.
  - [ ] Dropdown-поиск по временным зонам IANA.
  - [ ] Переключатель 12-часового (AM/PM) и 24-часового формата времени.
  - [ ] Реактивный пересчет времени слотов в сетке без перезагрузки страницы.
- [ ] **Компонент `CalendarMonthPicker`**
  - [ ] Интерактивная сетка месяца с переключением периодов (вперед/назад).
  - [ ] Блокировка (`disabled`) прошедших дат и дней без свободных слотов.
  - [ ] Визуальный индикатор выбранного дня.
- [ ] **Компонент `AvailableSlotsGrid`**
  - [ ] Отображение доступных временных окон на выбранный день.
  - [ ] Скелетон загрузки (`LoadingSkeleton`) при запросе слотов на бэкенд.
  - [ ] Выбор слота и раскрытие кнопки перехода к оформлению («Далее / Забронировать»).

---

### 1.2. Форма ввода данных: `BookingFormModal` / `/book/:slug/details`
- [ ] **Валидация полей формы (`react-hook-form` + `zod`):**
  - [ ] `client_name` (строка, required, min 2 символа, trim).
  - [ ] `client_email` (строка, email format, required).
  - [ ] `client_phone` (строка, опционально, валидация по международной маске).
  - [ ] `notes` (текстовая область, опционально, max 500 символов).
  - [ ] `guests` (массив email-адресов с добавлением по нажатию Enter).
  - [ ] Чекбокс согласия с правилами сервиса / обработкой ПДн.
- [ ] **UX и состояния отправки:**
  - [ ] Блокировка кнопки сабмита от повторных кликов (`isSubmitting`).
  - [ ] Передача `Idempotency-Key` в заголовке запроса.
  - [ ] Обработка гонки слотов (`HTTP 409 Conflict`): показ алерта *"Этот слот только что заняли"* и автоматическое обновление сетки времени.

---

### 1.3. Экран подтверждения: `/booking/:uuid/confirmed`
- [ ] **Информационный блок бронирования:**
  - [ ] Название встречи, аватар и имя организатора.
  - [ ] Дата и время (с указанием часового пояса клиента).
  - [ ] Ссылка на конференцию (Google Meet, Zoom) или адрес.
- [ ] **Интеграция с внешними календарями:**
  - [ ] Кнопка генерации прямой ссылки для Google Calendar (`calendar.google.com/calendar/render?...`).
  - [ ] Кнопка выгрузки файла `.ics` (для Apple Calendar и Outlook).
- [ ] **Навигация:**
  - [ ] Ссылка «Перенести встречу» (`/booking/:uuid/reschedule`).
  - [ ] Ссылка «Отменить встречу» (`/booking/:uuid/cancel`).

---

## 2. Управление бронированием клиентом (Self-Service) — Приоритет P1

### 2.1. Страница / Форма отмены: `/booking/:uuid/cancel`
- [ ] Отображение деталей отменяемой встречи.
- [ ] Выпадающий список / текстовое поле `cancellation_reason` (причина отмены).
- [ ] Модальное окно подтверждения («Вы уверены, что хотите отменить бронирование?»).
- [ ] Вызов бэкенд-метода `POST /api/bookings/:uuid/cancel`.
- [ ] Экран подтверждения успешной отмены.

### 2.2. Страница переноса: `/booking/:uuid/reschedule`
- [ ] Отображение текущего забронированного времени.
- [ ] Календарная сетка для выбора новой даты и времени.
- [ ] Вызов атомарного метода переноса (освобождение старого слота + захват нового без потери данных).

---

## 3. Панель управления организатора (Host Dashboard) — Приоритет P1

### 3.1. Страница настройки доступности: `/admin/availability`
- [ ] **Форма расписания по дням недели (`WeeklyScheduleForm`):**
  - [ ] Переключатели (Toggle) для каждого дня: Понедельник — Воскресенье.
  - [ ] Динамическое добавление/удаление интервалов времени (например: `09:00 - 13:00` и `14:00 - 18:00`).
  - [ ] Кнопка «Скопировать расписание понедельника на все будние дни».
- [ ] **Форма правил бронирования (`BookingRulesForm`):**
  - [ ] `slot_duration`: селектор продолжительности встречи (15, 30, 45, 60 мин).
  - [ ] `buffer_before` и `buffer_after`: время перерыва между слотами (0–60 мин).
  - [ ] `min_booking_notice`: минимальный срок до встречи (например, не раньше чем за 2 часа).
  - [ ] `max_future_days`: глубина календаря (например, доступность на 14 / 30 / 60 дней вперед).

---

### 3.2. Страница типов встреч: `/admin/event-types` и `/admin/event-types/new`
- [ ] Список активных типов встреч с тумблером вкл/выкл и кнопкой быстрого копирования ссылки.
- [ ] **Форма создания/редактирования типа встречи (`EventForm`):**
  - [ ] Название события (`title`).
  - [ ] URL-слаг (`slug`, с автогенерацией из названия и валидацией).
  - [ ] Описание встречи (`description`).
  - [ ] Способ связи (`location_type`: Google Meet, Zoom, Телефон, Офлайн).

---

### 3.3. Журнал бронирований: `/admin/bookings`
- [ ] Таблица броней с переключением табов:
  - [ ] **Upcoming** (Предстоящие).
  - [ ] **Past** (Прошедшие).
  - [ ] **Canceled** (Отмененные).
- [ ] Поиск по имени и email участника.
- [ ] Кнопка ручной отмены встречи организатором.
- [ ] **Модальное окно ручной блокировки времени (`BlockTimeModal`):**
  - [ ] Форма блокировки произвольного окна (отпуск, личные дела) без оформления брони на клиента.